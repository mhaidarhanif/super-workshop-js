# Workshop JS Spec :gear: API Blueprint

## Development

Make sure you have installed the latest Node.js and npm. Afterwards, install the required global dependencies.

```sh
npm i -g aglio
```

## Running

```sh
./apiblueprint.sh
```

Open the documentation on `http://localhost:3030`
